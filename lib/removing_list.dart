
/// Removeing List

void main() {

  var numbers=[1,2,3,4,5,6,];
  numbers.removeAt(0); //remove list with index
  numbers.removeLast();
  numbers.remove(1); //remove list items with item
  print(numbers);

}